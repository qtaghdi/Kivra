# @kivra/database

Supabase schema, migrations, generated database types, and database helpers.

Database models use snake_case and are transformed into application models inside service layers.

Run migrations from `supabase/migrations` when a Supabase project is linked.
